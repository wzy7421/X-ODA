[project]
name = "x-oda-revision-support"
version = "0.1.0"
description = "Revision-support scripts and privacy utilities for the X-ODA manuscript."
requires-python = ">=3.10"
dependencies = [
  "pandas>=2.0",
  "numpy>=1.24",
  "matplotlib>=3.7",
]

[tool.setuptools.packages.find]
where = ["src"]

